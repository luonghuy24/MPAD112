json.extract! program,  :name, :code, :school,
                        :credential, :program_type, :program_length,
                        :start_date, :location, :email, :telephone
# json.url program_url(program, format: :json)
